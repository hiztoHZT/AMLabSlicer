<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Theme Colors -->
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#202020" />
    <SolidColorBrush x:Key="TitleBarBackgroundBrush" Color="#18181A" />
    <SolidColorBrush x:Key="ToolbarBackgroundBrush" Color="#2D2D30" />
    
    <SolidColorBrush x:Key="PrimaryTextBrush" Color="#F1F1F1" />
    <SolidColorBrush x:Key="SecondaryTextBrush" Color="#A5A5A5" />
    
    <SolidColorBrush x:Key="ButtonHoverBrush" Color="#3E3E42" />
    <SolidColorBrush x:Key="ButtonBackgroundBrush" Color="Transparent" />
    <SolidColorBrush x:Key="CloseButtonHoverBrush" Color="#E81123" />
    
    <SolidColorBrush x:Key="MenuItemBackgroundBrush" Color="#1E1E1E" />
    <SolidColorBrush x:Key="MenuItemHoverBrush" Color="#3E3E42" />
    <SolidColorBrush x:Key="MenuItemTextBrush" Color="#F1F1F1" />

    <SolidColorBrush x:Key="GridSplitterBrush" Color="#3E3E42" />
    <SolidColorBrush x:Key="ViewportBackgroundBrush" Color="#222225" />

    <!-- Control Styles -->
    <Style x:Key="WindowButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource ButtonBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource ButtonHoverBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CloseButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource ButtonBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource CloseButtonHoverBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Menu / MenuItem Settings -->
    <Style TargetType="Menu">
        <Setter Property="Background" Value="Transparent"/>
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="FontFamily" Value="Microsoft YaHei" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Padding" Value="15,8" />
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="Foreground" Value="{DynamicResource MenuItemTextBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        
        <Style.Triggers>
            <Trigger Property="Role" Value="TopLevelHeader">
                <Setter Property="Padding" Value="10,0"/>
            </Trigger>
            <Trigger Property="Role" Value="SubmenuItem">
                <Setter Property="Background" Value="{DynamicResource MenuItemBackgroundBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource MenuItemTextBrush}"/>
                <Setter Property="MinWidth" Value="120"/>
            </Trigger>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{DynamicResource MenuItemHoverBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
